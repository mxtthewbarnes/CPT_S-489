# Database

`campus_closet.sqlite` is the SQLite database artifact for the project submission.

- Included file: `campus_closet.sqlite`
- Reset command from the repo root: `npm run seed`

If the reviewer wants a fresh database state, running the seed command will recreate this file with the demo dataset.
